* **Distributes network traffic** across **Pods or Nodes** via a **Service**.
* **Service** = stable **network endpoint** (IP + DNS) for a set of Pods.
* **Traffic distribution** algorithms: **Round-Robin, Least Connections, IP Hash**.
* Ensures **efficient & reliable traffic flow** across the cluster.

