# What's a pod ?
- smallest deployable unit that represent a single instance running process in a cluster
- pod can contain one or more container that share the same network namespace and can access the same storage volumes.
![[Pasted image 20250924224151.png]]